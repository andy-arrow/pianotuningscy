node_modules/
dist/
.astro/
.DS_Store
*.log
.env
.env.production

# iCloud Drive must not sync these — conflict copies ("index 2.html")
# corrupt the build output.
.audit/

# Internal deliverables — a critique of the client's business plus customer
# names taken from the old site. Not for a public repository.
AUDIT.md
audit-findings.json

# Review-deploy artefacts
preview-dist/
*.zip

# Cloudflare Worker local state
.wrangler/
